<%- include("../partials/header.ejs") %>
  <div class="container-fluid">
    <div class="card">
      <div class="card-header">
        <h2>View Attendance <a class="btn btn-primary float-right m-2" href="/medapp-attendance-admin/dashboard">Back to dahsboard</a> <a class="btn btn-primary float-right m-2" href="/medapp-attendance-admin/logout">Logout</a></h2>
        <h4><%= event.name %>, <%= event.date.toDateString() %></h4>
      </div>
      <div class="card-body">
        <table class="table table-striped">
          <thead>
            <tr>
              <th scope="col">Employee Name</th>
              <th scope="col">Email</th>
              <th scope="col">Designation</th>
              <th scope="col">Department</th>
              <th scope="col">Avatar</th>
            </tr>
          </thead>
          <tbody>
            <% if(employees.length > 0) { %>
              <% employees.forEach((employee) => { %>
                <tr>
                  <td><%= employee.name %></td>
                  <td><%= employee.email %></td>
                  <td><%= employee.designation || "Nil" %></td>
                  <td><%= employee.department || "Nil" %></td>
                  <td><a href="/medapp-attendance-admin/employee/avatar?emp=<%= employee._id %>"><img src="/medapp-attendance-admin/employee/avatar?emp=<%= employee._id %>" style="width: 100px"></a></td>
                </tr>
              <% }) %>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
  <div>
  </div>
<%- include("../partials/footer.ejs") %>